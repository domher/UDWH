ID: UDWH
_schema-version: '2.0'
version: 0.0.1

modules:
 - name: udwh_cl_ca
   type: hdb
   path: udwh_cl_ca
   requires:
    - name: hdi_udwh_cl_ca

resources:
 - name: hdi_udwh_cl_ca
   parameters:
      config:
         schema: UDWH
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
